# Acqz
## Development checks

- `npm run typecheck` validates all TypeScript files under `src/`.
- `npm run typecheck:dedupe` validates only `src/core/dedupe/leadDedupe.ts` with explicit Node/ES compiler flags.
